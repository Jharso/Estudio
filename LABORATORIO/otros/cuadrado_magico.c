//Backtracking Iterativo: Cuadrado Magico
#include <stdio.h>
#include <stdlib.h>

int main(){
	int matriz[N][N]; //cuadrado magico
	//[N][N] la 1) casilla filas y la 2) columnas
	suma_columnas(); 
	suma_filas(); 
	suma_diagonales(); 
	rellenar_matriz(int matriz[N][N]);
}

//backtracking iterativo
int opc = 1; 
int solucion=false;
while(opc<=matriz[N][N] && solucion=false){
	if(valido){
		//avanzar o agregar valor a una variable
		if(final){
			solucion=true;
		}
		else{
			//retroceder
		}
		opc++;
	}
}

int valido();
	int crear_cuadrado(int matriz[N][N]);
	

void crear_cuadrado(int cuadrado[n][n]){
	matriz[N][N]=NULL;
	if(matriz[N][N]==NULL){
		int i=1, j=1;
		
		

int suma_columnas(int filas, columnas){
	while(suma_columnas){
	
	}		
}
int suma_filas(){

}

int suma_diagonales()
	while(

