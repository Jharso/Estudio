#include <stdio.h>
#include<stdlib.h>
#include<time.h>
int main (){
	int a[20],x,i,aux;
	i=1;x=1;
	srand(time(NULL));
	while(i<=20){
		x=1+rand()%(100-1);
		a[i]=x;
		printf("arreglo numero[%d]=%d\n",i,a[i]);
		i=i+1;    }
